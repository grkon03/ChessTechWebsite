version: '3' # docker-composeのバージョンを指定
services: 
  nginx: # サービス名
    container_name: "nginx"
    image: nginx:1.14
    links:
      - php 
  php:
    container_name: "php"
    image: php:7.2-apache
    ports:
      - '80:80'
    volumes:
      - .:/var/www/html